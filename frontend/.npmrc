# Prevents npm from hard-failing on peer dependency conflicts.
# Next.js 14 has known peer dep warnings with some packages;
# this makes npm resolve them gracefully instead of erroring.
legacy-peer-deps=true
